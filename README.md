THE CURRENT WORKING MODEL OF THIS WEBSITE IS DEPLOYED ON CLOULD USING GOOGLE CLOUD PLATFORM
The web address for this domain os https://book-registry-903378632882.us-west1.run.app/

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
